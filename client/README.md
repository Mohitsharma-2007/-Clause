# Clause Frontend

This is the React frontend for the Clause compliance platform.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Run development server:
   ```bash
   npm run dev
   ```
   Access at `http://localhost:5173`.

## Configuration
See `.env.example` (copy to `.env`).
